import { Metadata } from "next"

export const metadata: Metadata = {
  title:'Blogs',
  description: 'Blogs page of portfolio website of Sanket Sarkar (sscodes)',
}

const page = () => {
  return (
    <div>
      blogs
    </div>
  )
}

export default page
